<h1><%= @attraction.name %>

<%= form_for @attraction, :url => "#{@user.id}/ride/#{@attraction.id}", :method => :post do |f| %>
	<%= f.submit "Go on this ride" %>
<% end %>

<% if current_user.admin %>
	<%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% end %>